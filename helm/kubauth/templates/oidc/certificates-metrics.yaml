{{- if .Values.oidc.enabled }}

{{- if  .Values.oidc.metrics.enabled }}

{{- if not .Values.oidc.metrics.certificateClusterIssuer }}
---
apiVersion: cert-manager.io/v1
kind: Issuer
metadata:
  name: {{ include "kubauth.oidc.metrics.certificateSelfSignedIssuerName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "kubauth.labels" . | nindent 4 }}
  {{- with .Values.commonAnnotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  selfSigned: {}
{{- end }}

---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ include "kubauth.oidc.metrics.certificateName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "kubauth.labels" . | nindent 4 }}
  {{- with .Values.commonAnnotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  dnsNames:
    - {{ include "kubauth.oidc.metrics.serviceName" . }}.{{ .Release.Namespace }}.svc
    - {{ include "kubauth.oidc.metrics.serviceName" . }}.{{ .Release.Namespace }}.svc.cluster.local
  issuerRef:
{{- if not .Values.oidc.metrics.certificateClusterIssuer }}
    kind: Issuer
    name: {{ include "kubauth.oidc.metrics.certificateSelfSignedIssuerName" . }}
{{- else }}
    kind: ClusterIssuer
    name: {{ .Values.oidc.metrics.certificateClusterIssuer }}
{{- end }}
  secretName: {{ include "kubauth.oidc.metrics.secretName" . }}

{{- end }}
{{- end }}
