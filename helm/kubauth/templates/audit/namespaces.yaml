{{- if .Values.audit.enabled }}
{{- if .Values.audit.createNamespace }}
---
apiVersion: v1
kind: Namespace
metadata:
  name: {{ .Values.audit.namespace }}
  labels:
    {{- include "kubauth.labels" . | nindent 4 }}
  {{- with .Values.commonAnnotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec: {}
{{- end }}
{{- end }}


