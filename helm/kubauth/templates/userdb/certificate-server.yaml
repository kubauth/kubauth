{{- if .Values.userdb.enabled }}
{{- if .Values.userdb.server.tls }}
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ include "kubauth.userdb.server.certificateName" . }}
  labels:
    {{- include "kubauth.labels" . | nindent 4 }}
  {{- with .Values.commonAnnotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  dnsNames:
    - {{ include "kubauth.userdb.server.serviceName" . }}
    - {{ include "kubauth.userdb.server.serviceName" . }}.{{ .Release.Namespace }}.svc
    - localhost
  {{- if .Values.userdb.ingress.enabled }}
    - {{ required "'userdb.ingress.host' must be defined if ingress is enabled" .Values.userdb.ingress.host }}
  {{- end }}
  secretName: {{ include "kubauth.userdb.server.certificateSecretName" . }}
  issuerRef:
    kind: {{ required ".Values.userdb.server.issuerKind is not defined while tls: true" .Values.userdb.server.issuerKind }}
    name: {{ required ".Values.userdb.server.certificateIssuer  is not defined while tls: true" .Values.userdb.server.certificateIssuer }}
{{- end }}
{{- end }}
