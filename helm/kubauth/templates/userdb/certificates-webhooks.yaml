{{- if .Values.userdb.enabled }}
{{- if .Values.userdb.webhooks.enabled }}
{{- if not .Values.userdb.webhooks.certificateClusterIssuer }}
---
apiVersion: cert-manager.io/v1
kind: Issuer
metadata:
  name: {{ include "kubauth.userdb.webhooks.certificateSelfSignedIssuerName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "kubauth.labels" . | nindent 4 }}
  {{- with .Values.commonAnnotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  selfSigned: {}
{{- end }}

---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ include "kubauth.userdb.webhooks.certificateName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "kubauth.labels" . | nindent 4 }}
  {{- with .Values.commonAnnotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  dnsNames:
    - {{ include "kubauth.userdb.webhooks.serviceName" . }}.{{ .Release.Namespace }}.svc
    - {{ include "kubauth.userdb.webhooks.serviceName" . }}.{{ .Release.Namespace }}.svc.cluster.local
  issuerRef:
{{- if not .Values.userdb.webhooks.certificateClusterIssuer }}
    kind: Issuer
    name: {{ include "kubauth.userdb.webhooks.certificateSelfSignedIssuerName" . }}
{{- else }}
    kind: ClusterIssuer
    name: {{ .Values.userdb.webhooks.certificateClusterIssuer }}
{{- end }}
  secretName: {{ include "kubauth.userdb.webhooks.secretName" . }}

{{- end }}
{{- end }}
