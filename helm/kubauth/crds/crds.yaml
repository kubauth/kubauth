apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: groupbindings.kubauth.kubotal.io
spec:
  group: kubauth.kubotal.io
  names:
    kind: GroupBinding
    listKind: GroupBindingList
    plural: groupbindings
    shortNames:
    - gb
    - gbs
    - kgb
    - kgbs
    - kgroupbinding
    - kgroupbindings
    singular: groupbinding
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.user
      name: User
      type: string
    - jsonPath: .spec.group
      name: Group
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: GroupBinding is the Schema for the groupbindings API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              group:
                minLength: 1
                type: string
              user:
                minLength: 1
                type: string
            required:
            - group
            - user
            type: object
          status:
            description: GroupBindingStatus defines the observed state of GroupBinding
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: groups.kubauth.kubotal.io
spec:
  group: kubauth.kubotal.io
  names:
    kind: Group
    listKind: GroupList
    plural: groups
    shortNames:
    - kgroup
    - kgroups
    singular: group
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.comment
      name: Comment
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              claims:
                x-kubernetes-preserve-unknown-fields: true
              comment:
                description: Whatever extra information related to this group.
                type: string
            type: object
          status:
            description: GroupStatus defines the observed state of Group
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: loginattempts.kubauth.kubotal.io
spec:
  group: kubauth.kubotal.io
  names:
    kind: LoginAttempt
    listKind: LoginAttemptList
    plural: loginattempts
    shortNames:
    - la
    singular: loginattempt
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.user.login
      name: Login
      type: string
    - jsonPath: .spec.user.name
      name: Name
      type: string
    - jsonPath: .spec.status
      name: Status
      type: string
    - jsonPath: .spec.authority
      name: Authority
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              authority:
                description: The module which validate the credentials
                type: string
              details:
                items:
                  properties:
                    provider:
                      properties:
                        claimAuthority:
                          type: boolean
                        credentialAuthority:
                          type: boolean
                        emailAuthority:
                          type: boolean
                        groupAuthority:
                          type: boolean
                        name:
                          type: string
                        nameAuthority:
                          type: boolean
                      required:
                      - claimAuthority
                      - credentialAuthority
                      - emailAuthority
                      - groupAuthority
                      - name
                      - nameAuthority
                      type: object
                    status:
                      type: string
                    translated:
                      properties:
                        claims:
                          x-kubernetes-preserve-unknown-fields: true
                        groups:
                          items:
                            type: string
                          type: array
                        uid:
                          type: integer
                      required:
                      - claims
                      - groups
                      type: object
                    user:
                      properties:
                        claims:
                          x-kubernetes-preserve-unknown-fields: true
                        emails:
                          items:
                            type: string
                          type: array
                        groups:
                          items:
                            type: string
                          type: array
                        login:
                          type: string
                        name:
                          type: string
                        uid:
                          type: integer
                      required:
                      - login
                      type: object
                  required:
                  - provider
                  - status
                  - translated
                  - user
                  type: object
                type: array
              status:
                description: The current status
                type: string
              user:
                description: The resulting user
                properties:
                  claims:
                    x-kubernetes-preserve-unknown-fields: true
                  emails:
                    items:
                      type: string
                    type: array
                  groups:
                    items:
                      type: string
                    type: array
                  login:
                    type: string
                  name:
                    type: string
                  uid:
                    type: integer
                required:
                - login
                type: object
              when:
                format: date-time
                type: string
            required:
            - status
            - user
            - when
            type: object
          status:
            description: LoginAttemptStatus defines the observed state of LoginAttempt
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: oidcclients.kubauth.kubotal.io
spec:
  group: kubauth.kubotal.io
  names:
    kind: OidcClient
    listKind: OidcClientList
    plural: oidcclients
    singular: oidcclient
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.clientId
      name: client_id
      type: string
    - jsonPath: .status.phase
      name: Status
      type: string
    - jsonPath: .status.message
      name: Message
      type: string
    - jsonPath: .spec.public
      name: Pub.
      type: boolean
    - jsonPath: .spec.description
      name: Description
      type: string
    - jsonPath: .spec.displayName
      name: Display
      type: string
    - jsonPath: .spec.entryURL
      name: Link
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of OidcClient
            properties:
              accessTokenLifespan:
                type: string
              audiences:
                items:
                  type: string
                type: array
              clientId:
                description: |-
                  The client_id for the OIDC protocol.
                  Default to:
                  - The resource name if the resource is defined in the clientPrivilegedNamespace
                  - <namespace>-<resourceName> in other cases
                  These default values ensure uniqueness of the client_id. If you override it by setting this value, you are responsible to maintains this uniqueness.
                  So, the recommended pattern is to NOT set this values.
                  NB: The effective clientId, used by the OIDC protocol is set in the Status part.
                type: string
              description:
                description: A human oriented description
                type: string
              displayName:
                description: Application name. May be used to be displayed in an application
                  list
                type: string
              entryURL:
                description: The application entry URL. May be used to build a user-friendly
                  list
                type: string
              forceOpenIdScope:
                description: Force openid scope, even if not requested.
                type: boolean
              grantTypes:
                description: The client's allowed grant types.
                items:
                  type: string
                type: array
              idTokenLifespan:
                type: string
              postLogoutURL:
                description: |-
                  Where to redirected user on logout.
                  Will take precedence on the same global configuration value.
                  May be overridden by a query parameter on the logout url
                type: string
              public:
                default: false
                description: true, if this client is marked as public.
                type: boolean
              redirectURIs:
                description: The client's allowed redirect URIs.
                items:
                  type: string
                type: array
              refreshTokenLifespan:
                type: string
              responseTypes:
                description: |-
                  The client's allowed response types.
                  All allowed combinations of response types have to be listed, each combination having
                  response types of the combination separated by a space.
                items:
                  type: string
                type: array
              scopes:
                description: The scopes this client is allowed to request.
                items:
                  type: string
                type: array
              secrets:
                description: |-
                  A list of k8s secret hosting the client_secret.
                  Required for non public oidcClient
                items:
                  properties:
                    hashed:
                      default: false
                      description: true, if the value is hashed (More secured)
                      type: boolean
                    key:
                      type: string
                    name:
                      type: string
                  required:
                  - hashed
                  - key
                  - name
                  type: object
                type: array
            required:
            - grantTypes
            - public
            - redirectURIs
            - responseTypes
            - scopes
            type: object
          status:
            description: status defines the observed state of OidcClient
            properties:
              clientId:
                type: string
              message:
                type: string
              phase:
                type: string
            required:
            - clientId
            - phase
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: ssosessions.kubauth.kubotal.io
spec:
  group: kubauth.kubotal.io
  names:
    kind: SsoSession
    listKind: SsoSessionList
    plural: ssosessions
    singular: ssosession
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.login
      name: Login
      type: string
    - jsonPath: .spec.fullName
      name: Name
      type: string
    - jsonPath: .spec.deadline
      name: Deadline
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          SsoSession is the Schema for the ssosessions API
          The name is the session token (The session cookie value)
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of SsoSession
            properties:
              claims:
                description: The OIDC Claims
                x-kubernetes-preserve-unknown-fields: true
              deadline:
                description: The absolute deadline, from SessionManager.Lifetime
                format: date-time
                type: string
              expiry:
                description: |-
                  time limit from SessionManage.IdleTimeout
                  IdleTimeout is meaningless, as this session is cross application. So it is unactivated in most case.
                  Anyway, we store this for technical coherency, even if it will be same as deadLine in all cases.
                format: date-time
                type: string
              fullName:
                description: The user full name
                type: string
              login:
                type: string
              webToken:
                description: |-
                  The original token (cookie value) of the web session.
                  Used as the SsoSession name in a sanitized non-reversible version of this
                type: string
            required:
            - deadline
            - expiry
            - login
            - webToken
            type: object
          status:
            description: status defines the observed state of SsoSession
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: upstreamproviders.kubauth.kubotal.io
spec:
  group: kubauth.kubotal.io
  names:
    kind: UpstreamProvider
    listKind: UpstreamProviderList
    plural: upstreamproviders
    shortNames:
    - upstreams
    singular: upstreamprovider
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Status
      type: string
    - jsonPath: .status.message
      name: Message
      type: string
    - jsonPath: .spec.clientId
      name: Client_id
      type: string
    - jsonPath: .spec.issuerURL
      name: Issuer_url
      type: string
    - jsonPath: .spec.redirectURL
      name: Redirect_url
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              certificateAuthority:
                description: CertificateAuthority references a PEM-encoded CA bundle
                  used to verify TLS to the upstream issuer (e.g. private PKI).
                properties:
                  configMap:
                    description: ConfigMap containing the CA bundle. Mutually exclusive
                      with Secret.
                    properties:
                      name:
                        default: ""
                        description: |-
                          Name of the referent.
                          This field is effectively required, but due to backwards compatibility is
                          allowed to be empty. Instances of this type with an empty value here are
                          almost certainly wrong.
                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                        type: string
                    type: object
                    x-kubernetes-map-type: atomic
                  key:
                    description: Key within the referenced ConfigMap or Secret. Defaults
                      to "ca.crt" if empty.
                    type: string
                  secret:
                    description: Secret containing the CA bundle. Mutually exclusive
                      with ConfigMap.
                    properties:
                      name:
                        default: ""
                        description: |-
                          Name of the referent.
                          This field is effectively required, but due to backwards compatibility is
                          allowed to be empty. Instances of this type with an empty value here are
                          almost certainly wrong.
                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                        type: string
                    type: object
                    x-kubernetes-map-type: atomic
                type: object
              clientId:
                type: string
              clientSecret:
                properties:
                  key:
                    description: Key within the referenced ConfigMap or Secret.
                    type: string
                  secret:
                    description: K8s Secret containing the secret value.
                    properties:
                      name:
                        default: ""
                        description: |-
                          Name of the referent.
                          This field is effectively required, but due to backwards compatibility is
                          allowed to be empty. Instances of this type with an empty value here are
                          almost certainly wrong.
                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                        type: string
                    type: object
                    x-kubernetes-map-type: atomic
                required:
                - key
                - secret
                type: object
              clientSpecific:
                default: false
                description: If set, will appear only if in the client upstream list
                type: boolean
              displayName:
                description: |-
                  Name prompted to the user
                  Required for all but internal. When internal, "" means display the dialog. A value means display a button
                type: string
              dumpExchanges:
                default: false
                description: For debugging
                type: boolean
              explicitConfig:
                description: |-
                  Allow to replace missing configuration discovery. Or to fix uncorrected value
                  It all or nothing -> if != nil take this config and don't perform discovery. If == nil, use discovery.
                properties:
                  authorization_endpoint:
                    description: IssuerURL string `json:"issuer"`
                    type: string
                  device_authorization_endpoint:
                    type: string
                  end_session_endpoint:
                    type: string
                  id_token_signing_alg_values_supported:
                    items:
                      type: string
                    type: array
                  introspection_endpoint:
                    description: The following are not provided by the github.com/coreos/go-oidc/v3/oidc
                      library, directly
                    type: string
                  jwks_uri:
                    type: string
                  token_endpoint:
                    type: string
                  userinfo_endpoint:
                    type: string
                type: object
              insecureSkipVerify:
                default: false
                description: Allow to skip server certificate validation
                type: boolean
              issuerURL:
                description: The issuer URL
                type: string
              redirectURL:
                type: string
              scopes:
                items:
                  type: string
                type: array
              type:
                description: The upstream type.
                enum:
                - oidc
                - internal
                type: string
            required:
            - type
            type: object
          status:
            description: UpstreamProviderStatus defines the observed state of User
            properties:
              effectiveConfig:
                description: The resulting configuration in case of discovery
                properties:
                  authorization_endpoint:
                    description: IssuerURL string `json:"issuer"`
                    type: string
                  device_authorization_endpoint:
                    type: string
                  end_session_endpoint:
                    type: string
                  id_token_signing_alg_values_supported:
                    items:
                      type: string
                    type: array
                  introspection_endpoint:
                    description: The following are not provided by the github.com/coreos/go-oidc/v3/oidc
                      library, directly
                    type: string
                  jwks_uri:
                    type: string
                  token_endpoint:
                    type: string
                  userinfo_endpoint:
                    type: string
                type: object
              message:
                type: string
              phase:
                type: string
            required:
            - phase
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: users.kubauth.kubotal.io
spec:
  group: kubauth.kubotal.io
  names:
    kind: User
    listKind: UserList
    plural: users
    shortNames:
    - kuser
    - kusers
    singular: user
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.name
      name: username
      type: string
    - jsonPath: .spec.emails
      name: Emails
      type: string
    - jsonPath: .spec.uid
      name: Uid
      type: integer
    - jsonPath: .spec.comment
      name: Comment
      type: string
    - jsonPath: .spec.disabled
      name: Disabled
      type: boolean
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: User is the Schema for the users API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              claims:
                description: The oidc Claims
                x-kubernetes-preserve-unknown-fields: true
              comment:
                description: Whatever extra information related to this user.
                type: string
              disabled:
                description: Prevent this user to login. Even if this user is managed
                  by an external provider (such as LDAP)
                type: boolean
              emails:
                description: |-
                  The user email(s).
                  First one is used as email claim
                items:
                  type: string
                type: array
              name:
                description: The user full name.
                type: string
              passwordHash:
                description: |-
                  The user password, Hashed. Using golang.org/x/crypto/bcrypt.GenerateFromPassword()
                  Is optional, in case we only enrich a user from another directory
                type: string
              uid:
                description: Numerical user id
                type: integer
            type: object
          status:
            description: UserStatus defines the observed state of User
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
