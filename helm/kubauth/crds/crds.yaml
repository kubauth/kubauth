apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: groupbindings.kubauth.kubotal.io
spec:
  group: kubauth.kubotal.io
  names:
    kind: GroupBinding
    listKind: GroupBindingList
    plural: groupbindings
    shortNames:
    - gb
    - gbs
    - skgroupbinding
    - skgroupbindings
    - skgb
    - skgbs
    - skasgb
    - skasgb
    singular: groupbinding
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.user
      name: User
      type: string
    - jsonPath: .spec.group
      name: Group
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: GroupBinding is the Schema for the groupbindings API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              group:
                minLength: 1
                type: string
              user:
                minLength: 1
                type: string
            required:
            - group
            - user
            type: object
          status:
            description: GroupBindingStatus defines the observed state of GroupBinding
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: groups.kubauth.kubotal.io
spec:
  group: kubauth.kubotal.io
  names:
    kind: Group
    listKind: GroupList
    plural: groups
    singular: group
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.comment
      name: Comment
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              claims:
                x-kubernetes-preserve-unknown-fields: true
              comment:
                description: Whatever extra information related to this group.
                type: string
            type: object
          status:
            description: GroupStatus defines the observed state of Group
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: oidcclients.kubauth.kubotal.io
spec:
  group: kubauth.kubotal.io
  names:
    kind: OidcClient
    listKind: OidcClientList
    plural: oidcclients
    singular: oidcclient
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.id
      name: ID
      type: string
    - jsonPath: .spec.public
      name: Pub.
      type: boolean
    - jsonPath: .spec.name
      name: Name
      type: string
    - jsonPath: .spec.description
      name: Description
      type: string
    - jsonPath: .spec.entryURL
      name: Link
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of OidcClient
            properties:
              audiences:
                items:
                  type: string
                type: array
              description:
                description: A human oriented description
                type: string
              displayName:
                description: Application name. May be used to be displayed in an application
                  list
                type: string
              entryURL:
                description: The application entry URL. May be used to build a user-friendly
                  list
                type: string
              grantTypes:
                description: The client's allowed grant types.
                items:
                  type: string
                type: array
              hashedSecret:
                description: The hashed secret. (Required if !public)
                type: string
              id:
                description: The client ID.
                type: string
              postLogoutURL:
                description: |-
                  Where to redirected user on logout.
                  Will take precedence on the same global configuration value.
                  May be overridden by a query parameter on the logout url
                type: string
              public:
                default: false
                description: true, if this client is marked as public.
                type: boolean
              redirectURIs:
                description: The client's allowed redirect URIs.
                items:
                  type: string
                type: array
              responseTypes:
                description: |-
                  The client's allowed response types.
                  All allowed combinations of response types have to be listed, each combination having
                  response types of the combination separated by a space.
                items:
                  type: string
                type: array
              scopes:
                description: The scopes this client is allowed to request.
                items:
                  type: string
                type: array
            required:
            - grantTypes
            - id
            - public
            - redirectURIs
            - responseTypes
            - scopes
            type: object
          status:
            description: status defines the observed state of OidcClient
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: ssosessions.kubauth.kubotal.io
spec:
  group: kubauth.kubotal.io
  names:
    kind: SsoSession
    listKind: SsoSessionList
    plural: ssosessions
    singular: ssosession
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.login
      name: Login
      type: string
    - jsonPath: .spec.fullName
      name: Name
      type: string
    - jsonPath: .spec.deadline
      name: Deadline
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          SsoSession is the Schema for the ssosessions API
          The name is the session token (The session cookie value)
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of SsoSession
            properties:
              claims:
                description: The OIDC Claims
                x-kubernetes-preserve-unknown-fields: true
              deadline:
                description: The absolute deadline, from SessionManager.Lifetime
                format: date-time
                type: string
              expiry:
                description: |-
                  time limit from SessionManage.IdleTimeout
                  IdleTimeout is meaningless, as this session is cross application. So it is unactivated in most case.
                  Anyway, we store this for technical coherency, even if it will be same as deadLine in all cases.
                format: date-time
                type: string
              fullName:
                description: The user full name
                type: string
              login:
                type: string
              webToken:
                description: |-
                  The original token (cookie value) of the web session.
                  Stored as the SsoSession name is a sanitized non-reversible version of this
                type: string
            required:
            - deadline
            - expiry
            - login
            - webToken
            type: object
          status:
            description: status defines the observed state of SsoSession
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: users.kubauth.kubotal.io
spec:
  group: kubauth.kubotal.io
  names:
    kind: User
    listKind: UserList
    plural: users
    shortNames:
    - kuser
    - kusers
    singular: user
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.commonNames
      name: Common names
      type: string
    - jsonPath: .spec.emails
      name: Emails
      type: string
    - jsonPath: .spec.uid
      name: Uid
      type: integer
    - jsonPath: .spec.comment
      name: Comment
      type: string
    - jsonPath: .spec.disabled
      name: Disabled
      type: boolean
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: User is the Schema for the users API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              claims:
                description: The oidc Claims
                x-kubernetes-preserve-unknown-fields: true
              comment:
                description: Whatever extra information related to this user.
                type: string
              commonNames:
                description: |-
                  The user common name(s).
                  First is used as fullName
                items:
                  type: string
                type: array
              disabled:
                description: Prevent this user to login. Even if this user is managed
                  by an external provider (i.e. LDAP)
                type: boolean
              emails:
                description: The user email(s).
                items:
                  type: string
                type: array
              passwordHash:
                description: |-
                  The user password, Hashed. Using golang.org/x/crypto/bcrypt.GenerateFromPassword()
                  Is optional, in case we only enrich a user from another directory
                type: string
              uid:
                description: Numerical user id
                type: integer
            type: object
          status:
            description: UserStatus defines the observed state of User
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
