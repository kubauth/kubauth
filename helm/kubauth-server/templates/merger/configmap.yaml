{{- if .Values.merger.enabled }}

---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "kubauth.merger.configMapName" . }}
  labels:
    {{- include "kubauth.labels" . | nindent 4 }}
  {{- with .Values.commonAnnotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
data:
  config.yaml: |
    providers:
      {{- range .Values.merger.providers }}
      - name: {{ .name }}
        httpConfig:
          baseURL: {{ .httpConfig.baseUrl }}
        credentialAuthority: {{ .credentialAuthority | default true }}
        groupAuthority: {{ .groupAuthority | default true }}
        groupPattern: "{{ .groupPattern | default "%s" }}"
        claimAuthority: {{ .claimAuthority | default true }}
        claimPattern: "{{ .claimPattern | default "%s" }}"
        nameAuthority: {{ .nameAuthority | default true }}
        emailAuthority: {{ .emailAuthority | default true }}
        critical: {{ .critical | default true }}
        uidOffset: {{ .uidOffset | default 0 }}
      {{- end }}

{{- end }}