{{- if .Values.ucrd.enabled }}


{{- if .Values.ucrd.metrics.enabled}}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "kubauth.ucrd.metrics.serviceName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "kubauth.labels" . | nindent 4 }}
  {{- with .Values.commonAnnotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  ports:
    - name: https
      port: {{ .Values.ucrd.metrics.bindPort }}
      protocol: TCP
      targetPort: {{ .Values.ucrd.metrics.bindPort }}
  selector:
    {{- include "kubauth.selectorLabels" . | nindent 6 }}
{{- end }}


{{- if .Values.ucrd.webhooks.enabled }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "kubauth.ucrd.webhooks.serviceName" .}}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "kubauth.labels" . | nindent 4 }}
  {{- with .Values.commonAnnotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  ports:
    - port: 443
      protocol: TCP
      targetPort: {{ .Values.ucrd.webhooks.port }}
  selector:
    {{- include "kubauth.selectorLabels" . | nindent 6 }}

{{- end }}

{{- end }}
