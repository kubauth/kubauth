{{- if .Values.ucrd.enabled }}
{{- if .Values.ucrd.webhooks.enabled }}
{{- if not .Values.ucrd.webhooks.certificateClusterIssuer }}
---
apiVersion: cert-manager.io/v1
kind: Issuer
metadata:
  name: {{ include "kubauth.ucrd.webhooks.certificateSelfSignedIssuerName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "kubauth.labels" . | nindent 4 }}
  {{- with .Values.commonAnnotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  selfSigned: {}
{{- end }}

---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ include "kubauth.ucrd.webhooks.certificateName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "kubauth.labels" . | nindent 4 }}
  {{- with .Values.commonAnnotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  dnsNames:
    - {{ include "kubauth.ucrd.webhooks.serviceName" . }}.{{ .Release.Namespace }}.svc
    - {{ include "kubauth.ucrd.webhooks.serviceName" . }}.{{ .Release.Namespace }}.svc.cluster.local
  issuerRef:
{{- if not .Values.ucrd.webhooks.certificateClusterIssuer }}
    kind: Issuer
    name: {{ include "kubauth.ucrd.webhooks.certificateSelfSignedIssuerName" . }}
{{- else }}
    kind: ClusterIssuer
    name: {{ .Values.ucrd.webhooks.certificateClusterIssuer }}
{{- end }}
  secretName: {{ include "kubauth.ucrd.webhooks.secretName" . }}

{{- end }}
{{- end }}
