{{- if .Values.logger.enabled }}
{{- if .Values.logger.server.tls }}
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ include "kubauth.logger.server.certificateName" . }}
  labels:
    {{- include "kubauth.labels" . | nindent 4 }}
  {{- with .Values.commonAnnotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  dnsNames:
    - {{ include "kubauth.logger.server.serviceName" . }}
    - {{ include "kubauth.logger.server.serviceName" . }}.{{ .Release.Namespace }}.svc
    - localhost
  {{- if .Values.logger.ingress.enabled }}
    - {{ required "'logger.ingress.host' must be defined if ingress is enabled" .Values.logger.ingress.host }}
  {{- end }}
  secretName: {{ include "kubauth.logger.server.certificateSecretName" . }}
  issuerRef:
    kind: {{ required ".Values.logger.server.issuerKind is not defined while tls: true" .Values.logger.server.issuerKind }}
    name: {{ required ".Values.logger.server.certificateIssuer  is not defined while tls: true" .Values.logger.server.certificateIssuer }}
{{- end }}
{{- end }}
