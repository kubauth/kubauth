---
# Per-test User CRD that COLLIDES on login `carol` with the LDAP user.
# Both authorities will respond; the merger composes the response per the
# claimAuthority/emailAuthority/groupAuthority flags in kubauth-values.yaml.
apiVersion: kubauth.kubotal.io/v1alpha1
kind: User
metadata:
  name: carol
  namespace: kubauth-users
spec:
  name: Carol from UCRD
  emails:
    - carol-ucrd@example.test
  passwordHash: "$2a$12$5nbfe7pS/7piy65qQIoKq.6GRo1jj/hUA2fBoN.COLVKL9R.K1MD."
  uid: 5001
  comment: "Used by 11-merger-claim-priority — collides with LDAP carol."
