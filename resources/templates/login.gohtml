<!DOCTYPE html>
<html lang="en"{{if eq .Style "light"}} class="theme-light"{{end}}>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Kubauth Login</title>
    <link rel="stylesheet" href="/static/shared.css">
    <link rel="stylesheet" href="/static/login.css">

</head>
<body>
<div class="login-container container">
    <div class="banner">
        <h1>Kubauth Sign-in</h1>
    </div>

    {{- if .ShowNoProviderMessage }}
    <div class="no-provider">
        <p>Sorry, no authentication provider configured.</p>
        <p>Check with your system administrator</p>
    </div>
    {{- end }}
    {{- if .UpstreamButtons}}
    <div class="upstream-providers" role="group" aria-label="External identity providers">
        {{- range .UpstreamButtons}}
        <a class="btn btn-upstream" href="/upstream/go?upstreamProvider={{.Name}}">{{.DisplayName}}</a>
        {{- end}}
    </div>
    {{- end}}

    {{- if .ShowLoginForm}}
    {{- if .ShowFieldset}}
    <div id="errorMessage" class="error-message">
        <!-- Error messages will be displayed here -->
    </div>
    <fieldset class="login-form-fieldset">
        <legend class="login-form-legend">{{.FieldsetLabel}}</legend>
        <form id="loginForm" method="POST" action="/oauth2/login">
    {{- else}}
        <form id="loginForm" method="POST" action="/oauth2/login">
    {{- end}}
            <div class="form-group">
                <label for="login">Login</label>
                <input type="text" id="login" name="login" required>
            </div>

            <div class="form-group password-group">
                <label for="password">Password</label>
                <input type="password" id="password" name="password" required>
                <span class="password-toggle" id="passwordToggle">
                        <!-- Eye icon (hidden) -->
                        <svg id="eyeIcon" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">
                            <path d="M12 4.5C7 4.5 2.73 7.61 1 12c1.73 4.39 6 7.5 11 7.5s9.27-3.11 11-7.5c-1.73-4.39-6-7.5-11-7.5zM12 17c-2.76 0-5-2.24-5-5s2.24-5 5-5 5 2.24 5 5-2.24 5-5 5zm0-8c-1.66 0-3 1.34-3 3s1.34 3 3 3 3-1.34 3-3-1.34-3-3-3z"/>
                        </svg>
                    <!-- Eye slash icon (visible) -->
                        <svg id="eyeSlashIcon" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" style="display: none;">
                            <path d="M12 7c2.76 0 5 2.24 5 5 0 .65-.13 1.26-.36 1.83l2.92 2.92c1.51-1.26 2.7-2.89 3.43-4.75-1.73-4.39-6-7.5-11-7.5-1.4 0-2.74.25-3.98.7l2.16 2.16C10.74 7.13 11.35 7 12 7zM2 4.27l2.28 2.28.46.46C3.08 8.3 1.78 10.02 1 12c1.73 4.39 6 7.5 11 7.5 1.55 0 3.03-.3 4.38-.84l.42.42L19.73 22 21 20.73 3.27 3 2 4.27zM7.53 9.8l1.55 1.55c-.05.21-.08.43-.08.65 0 1.66 1.34 3 3 3 .22 0 .44-.03.65-.08l1.55 1.55c-.67.33-1.41.53-2.2.53-2.76 0-5-2.24-5-5 0-.79.2-1.53.53-2.2zm4.31-.78l3.15 3.15.02-.16c0-1.66-1.34-3-3-3l-.17.01z"/>
                        </svg>
                    </span>
            </div>

            <div class="form-group">
                <label class="checkbox-container">
                    <input type="checkbox" id="remember" name="remember">
                    <span class="checkmark"></span>
                    Activate SSO
                </label>
            </div>

            <button type="submit" class="login-button btn">Login</button>
        </form>
    {{- if .ShowFieldset}}
    </fieldset>
    {{- end}}
    {{- end}}
</div>

<script>
    (function() {
        const passwordInput = document.getElementById('password');
        const passwordToggle = document.getElementById('passwordToggle');
        if (passwordInput && passwordToggle) {
            const eyeIcon = document.getElementById('eyeIcon');
            const eyeSlashIcon = document.getElementById('eyeSlashIcon');
            passwordToggle.addEventListener('click', function() {
                if (passwordInput.type === 'password') {
                    passwordInput.type = 'text';
                    eyeIcon.style.display = 'none';
                    eyeSlashIcon.style.display = 'block';
                } else {
                    passwordInput.type = 'password';
                    eyeIcon.style.display = 'block';
                    eyeSlashIcon.style.display = 'none';
                }
            });
        }

        const loginForm = document.getElementById('loginForm');
        if (loginForm) {
            loginForm.addEventListener('submit', function() {
                const username = document.getElementById('login').value;
                const password = document.getElementById('password').value;
                const errorDiv = document.getElementById('errorMessage');
                errorDiv.classList.remove('show');
                if (!username || !password) {
                    showError('Please fill in all fields.');
                    return;
                }
                if (username.length < 2) {
                    showError('Username must be at least 2 characters long.');
                    return;
                }
                if (password.length < 2) {
                    showError('Password must be at least 2 characters long.');
                    return;
                }
            });
        }

        function showError(message) {
            const errorDiv = document.getElementById('errorMessage');
            errorDiv.textContent = message;
            errorDiv.classList.add('show');
        }
        window.showLoginError = showError;
    })();
    {{- if .InvalidLogin}}
    if (typeof window.showLoginError === 'function') {
        window.showLoginError('Invalid username or password.');
    }
    {{- end}}
</script>

<footer class="footer-zone">
    <p>Version: {{ .Version}}  Build: {{ .BuildTs}}  Style: {{ .Style }}</p>
</footer>
</body>
</html>
